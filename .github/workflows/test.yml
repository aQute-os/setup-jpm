name: Tests

on:
  push:
    branches:
      - master
  pull_request:

defaults:
  run:
    shell: bash

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Java
        uses: actions/setup-java@v2
        with:
          distribution: "temurin"
          java-version: 8
      - name: Setup bnd
        id: bnd
        uses: ./
        with:
          version: 1
      - name: Test it
        run: ls .bin
        run: ./.bin/bnd version
        run: bnd version
